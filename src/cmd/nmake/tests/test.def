# default nmake regress(1) definitions

UNIT nmake

(vpath .) </dev/null >/dev/null 2>&1 && vpath - /#option/2d

unset A
unset NPROC
unset P
unset MAKESKIP
unset VPATH

export CC=cc
export ERROR_OPTIONS='nosystem'
export LANG=C LC_ALL=C
export MAKE_OPTIONS='nativepp=-1 prefixinclude=0'
export PS4='+ '
